import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PaginaAlumno2Page } from './pagina-alumno-2.page';

describe('PaginaAlumno2Page', () => {
  let component: PaginaAlumno2Page;
  let fixture: ComponentFixture<PaginaAlumno2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PaginaAlumno2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
